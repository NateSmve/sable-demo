/** @type {import('next').NextConfig} */
const nextConfig = {
  // Image optimization — add client's domain here if they use external images
  // images: {
  //   remotePatterns: [{ protocol: 'https', hostname: 'example.com' }],
  // },
};

module.exports = nextConfig;
